import { createTheme } from "@mui/material";

export const LightTheme = createTheme({
    palette: {
        primary: {
            main: '#000000'
        },
        secondary: {
            main: '#000000'
        },
        background: {
            default: "#e4f0e2"
        },
        text: {
            primary: "#000000",
            secondary: "#000000"
        }
    },
});